# Role `patch_baseline`

Controlled patch management baseline for Rocky Linux and FreeIPA nodes.

## Scope

- available updates check
- security-only mode by default
- report generation per host
- optional controlled patch application
- optional reboot after patching

Automatic updates are disabled by default.
