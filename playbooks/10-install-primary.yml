---
- name: Install FreeIPA primary
  hosts: idm_primary
  become: true

  roles:
    - common_hosts
    - chrony_server
    - ipa_primary
    - pki_baseline
    - ssh_hardening
    - audit_baseline
    - log_rotation
    - ipa_monitoring_baseline
