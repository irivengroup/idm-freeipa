---
- name: Run project release readiness check
  hosts: localhost
  connection: local
  become: false
  gather_facts: false

  vars:
    release_readiness_report_dir: "{{ playbook_dir | dirname }}/.reports/release-readiness"

  roles:
    - release_readiness_check
